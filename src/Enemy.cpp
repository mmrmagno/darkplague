#include "Enemy.h"

void Enemy::update(Player& player) {
    // Simple AI logic to follow and attack the player
}

GLuint Enemy::getVAO() {
    return vao;
}

int Enemy::getVertexCount() {
    return vertexCount;
}
